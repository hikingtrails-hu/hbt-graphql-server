import { pubsubConfig } from './pubsub'
import { cloudStorageConfig } from './cloud-storage'
import { httpConfig } from './http'
import { filterMapDataConfig } from './filter-map-data'
import { mapConfig } from './map'

export const config = {
    pubsubConfig,
    cloudStorageConfig,
    httpConfig,
    filterMapDataConfig,
    mapConfig
}
